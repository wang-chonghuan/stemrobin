.git
node_modules
app/node_modules
.agents/skills/node_modules
dist
app/dist
.output
app/.output
app/.tanstack
# secrets — never into the image (matches .env and the app/.env symlink at any depth)
**/.env
**/.env.*
npm-debug.log

# 构建上下文瘦身：az acr build 以仓库根为上下文，下面这些会把上传拖到数百 MB
# 工作区临时产物（PDF、页面渲染、数据库 dump），数百 MB，永远不该进构建上下文
.tmp
# 本地扫描件原件，不参与构建
resources
# agent 技能自带 python venv 与 node_modules（约 120MB），镜像里用不到
.claude
.agents
.codex
.opencode
# 教材抽取产物：内容走数据库，构建不读它
page2class
prototypes
test-results
