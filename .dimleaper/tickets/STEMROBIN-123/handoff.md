# STEMROBIN-123 · handoff

## What changed

内容工单，不改任何产品代码。

- **七篇课文入库**：`english-u02-08` … `english-u02-14`，即蓝图单元 02「上学路上与学校」的第 08–14 课。
  规格文件提交在 `resources/content/english/u02-08.json` … `u02-14.json`；正文、逐句中文、逐句朗读、
  整篇朗读、跟读练习音频、生词卡发音全部由 `save-lesson.mjs` 一次写入。
  - 08 `Crossing the Road`〔对话〕9 句 / 92 词 — Ben 与 Ana 两个说话人，两把嗓音
  - 09 `How I Go to School` 9 句 / 107 词
  - 10 `My School and My Classroom` 9 句 / 116 词
  - 11 `In Class` 9 句 / 106 词
  - 12 `My School Week` 8 句 / 91 词
  - 13 `The Math Test` 9 句 / 111 词
  - 14 `A Busy Day`〔综合〕9 句 / 119 词
- **138 个目标词分配到七课**（`resources/content/course-wordlist.json`：`lesson` + `state`），
  与单元 01 当初的做法一致 —— 六篇教学课各 22 词，综合课 6 词。分配在写课文之前完成，所以保存时的
  对账是拿计划比实教，而不是事后追认。
- 课程词表覆盖率 **12.06% → 20.95%**（209 → 363 条）。

写作顺序按技能规定：先从 `outline.md` 取场景与句型、把故事写成一个值得背的完整小场景，再向未覆盖词
靠拢；不是照词表拼句子。`outline.md` 一个字未动。

## AC results

| | 结果 | 观察到什么 |
|---|---|---|
| AC1 目录出现第 8–14 课、标题与蓝图一致 | **通过** | 浏览器侧栏「技术英语」计数 14，第 8–14 条依次是 Crossing the Road / How I Go to School / My School and My Classroom / In Class / My School Week / The Math Test / A Busy Day，与蓝图七个场景一一对上。 |
| AC2 每课 6–9 句、每句有中文和朗读；整篇与跟读音频可播 | **通过** | 以 12 课为例：正文首句在页，逐句「播放朗读」按钮 42 个，「听整篇」在，「下载跟读音频」控件在；点开「整篇中文对照」后中文出现（中文默认收在开关里，不是默认展开）。跟读音频实际取回：08/12/14 三课 `HTTP 200 audio/mpeg`，1.5–1.7 MB。 |
| AC3 逐级挖空默写可从第一级走到第五级 | **通过** | 五级名为 轻度/中度/重度/极限/全文默写。逐级点过去，挖空数 22 → 40 → 58 → 76 递增，每级都是「本级已过 0/8 句」；全文默写是整篇默写、无逐句空。与单元 01 的表现一致（对照 u01-01 / u01-07）。 |
| AC4 `reconcile.mjs --status` 退出码 0 | **通过** | `词表状态: {"unassigned":1457,"taught":276}`，无待处置漏词，退出码 0。七次保存每次都是「计划 22 词 / 实教 22 词 · 无漏词」（综合课 6/6）。 |

机械防线 `cd app && npm run test && npm run build`：75 个测试全过，构建通过。

**两次断言写错，都是检查本身的问题，不是产品的**：一是把中文当成默认可见（实际在「整篇中文对照」
开关后面）；二是把跟读控件按可见文字找（实际是纯图标，可访问名「下载跟读音频」）。还有一次
「极限级 0/0 句」是在切级重渲染中途取的数，等进度文案稳定后是 76 空 / 8 句。三处都改了检查再重跑，
没有一处是放宽判定。

## Deviations

- **中途停过一次，隔了一个工单才回来。** 第一轮保存写进了 `EASYAPP_DATABASE_URL` /
  `stemrobin-schema`，而 app 早已改读 `LEMMADECK_DATABASE_URL` / `lemmadeck-schema` —— 七篇课文
  「保存成功」却在产品里完全不存在。这不是本工单能修的（要改 5 个技能脚本），按 chore 的规矩停下
  交给人，人裁定以新库为准，另开 **STEMROBIN-124** 修管线。124 合并后，本工单只把七个规格重跑一遍
  `save-lesson.mjs` 即完成 —— 课文、分配、音频参数一个字没重写。
- 旧库里那七课的副本还在，属于 124 的遗留项，本工单不清理。

## Environment

- 用的端口：**52123**。
- 没有新增、修改或删除任何环境变量。

## Residual

- **`practice` 跟读音频每篇约 1.6 MB**，14 课已经约 22 MB 存在 `sr_lesson_audio` 里。84 课全部写完
  会到约 130 MB。是否该改为按需生成或走对象存储，值得单独评估。
- 单元 02 有两个词是 B1（`grade`、`proud`）—— 它们在课程词表里（蓝图点名、Oxford 归 B1 的那批），
  闸门放行。若希望教学课只用 A1/A2，需要人给一条规则，目前闸门不管级别。
- 词表覆盖率 20.95%，`复现 <3 篇` 还有 283 条。按每单元约 150 词推算，84 课能覆盖到的量与 1733 条
  的目标之间仍有差距，到中段值得重新核一次预算 —— 技能自己也说过，这种情况是停下报告，不是偷偷放宽。
