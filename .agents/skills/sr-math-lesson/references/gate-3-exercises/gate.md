# Gate 3 — Exercise deck

Run after cap3 (script already passed), before cap4 persists. Independent subagent, given: the deck JSON, the ledger, the lesson HTML, the contract.

## First cause

Two weeks from now, will this deck have made the learner *fast at naming*, *able to do*, and *able to explain* — or is it another pile of same-shaped procedure items a pattern-matcher can clear without forming a single category?

## Checks (fail on any) — the script already enforced shape/composition; judge MEANING

- **Answerable-without-understanding**: 指认 items whose surface form telegraphs the answer; operation items identical up to digit swaps; any run of items a child could clear by copying the previous answer's pattern.
- **Boundary theater**: boundary_cases nominally present but not actually testing the edge (e.g. asks 系数 of 3x instead of −x).
- **Lazy distractors**: any choice distractor not wrong-for-a-nameable-reason.
- **Review that isn't review**: 复习 items testing this lesson's content, or review_of terms that the item doesn't actually exercise.
- **说理 items that are recall**: "说理" items answerable by reciting a sentence from the 課文 rather than reconstructing the reason on an instance.
- **Answer keys that don't teach**: bare values with no why; or answers that are wrong (verify every correct_index and accept list by solving the item yourself — a wrong key is an automatic fail).
- **input misuse**: accept lists missing obvious legitimate variants (the learner types a right answer and gets marked wrong — worse than no item); or answers with genuinely many forms forced into input where choice/work fits better.

Solve every item. A single wrong key, wrong accept list, or mis-indexed correct answer fails the gate.

## Pass

Every key verified correct; edges genuinely tested; distractors diagnostic; review real; explanations modeled. Then cap4 may persist.
